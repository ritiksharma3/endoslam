__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# local venvs
.venv/
venv/

# DarkIR upstream repo -- prior phases only ever cloned this inside an
# ephemeral Kaggle container; local CLI usage (src/inference/reconstruct_video.py)
# clones it into this persistent working directory instead.
/DarkIR_upstream/

# data (never commit dataset contents; Kaggle mounts it, don't mirror it here)
/data/
*.ckpt
*.pth
checkpoints/
# checkpoints_resume/darkir_lite_latest.pt is committed on purpose -- it's
# the single resume checkpoint carrying Phase 2 training state across
# separate Kaggle sessions (see PROGRESS.md). Not matched by "checkpoints/"
# above (different directory name) or *.pth (it's .pt) -- keep it that way.

# outputs
outputs/
*.log

# sample/test videos for src/inference/reconstruct_video.py -- downloaded/
# generated locally, not project source, don't mirror external or
# dataset-derived video content here
/sample_videos/
